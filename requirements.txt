# Core FastAPI dependencies
fastapi[standard]==0.120.4
uvicorn[standard]==0.32.0
pydantic==2.11.7
pydantic-settings==2.6.0

# Database dependencies
sqlalchemy==2.0.23
alembic==1.12.1
PyMySQL==1.1.1
psycopg2-binary>=2.9.9

# Authentication & Security
# PyJWT, not python-jose. python-jose is unmaintained and pins pyasn1<0.5,
# which both made requirements.txt uninstallable and dragged in pyasn1's own
# advisories. PyJWT has no pyasn1 dependency, and HS256 tokens are byte-identical
# between the two, so sessions issued by the old library keep working.
PyJWT==2.13.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.32

# Validation & Utilities
email-validator==2.0.0
python-dotenv==1.2.2

# Production dependencies
gunicorn==23.0.0
psutil==7.0.0

# Monitoring & Logging
prometheus-client==0.19.0
structlog==23.2.0

# Security
cryptography==50.0.1

# Google Authentication & FCM
google-auth==2.40.3
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.2.0

# HTTP requests
requests==2.33.0
yt-dlp>=2026.6.9

# Timezone support
pytz==2024.1
apscheduler==3.10.4
a2wsgi==1.10.0

# OpenAI SDK for DeepSeek API
openai==1.55.0
httpx==0.27.2
pypdf==6.16.1
python-docx==1.1.2
openpyxl==3.1.2
python-pptx==1.0.2
Pillow>=11.0.0
easyocr==1.7.1

# Cloud Storage
cloudinary==1.41.0
boto3==1.35.0
firebase-admin==6.5.0
redis==5.0.3

# Telegram MTProto API (official OTP login via Telethon)
telethon>=1.36.0
phonenumbers==9.0.30
